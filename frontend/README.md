# DeFi Super-App — Frontend

React + Vite + Wagmi Telegram Mini App frontend.

## Setup

```bash
cd frontend
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Environment

The app targets **Arbitrum Sepolia**. Update contract addresses in `src/config/contracts.ts` after deployment.
